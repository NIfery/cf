<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.it.cf.question.model.QuestionDAO">
	<insert id="insertQuestion" parameterType="questionVo" >
		<selectKey keyProperty="questionNo" resultType="int" order="BEFORE">
			select question_SEQ.nextval from dual
		</selectKey>
		insert into QUESTION(QUESTION_NO,QUESTION_TITLE,QUESTION_CONTENT,QUESTION_FLAG,USER_NO,PROJECT_NO)
		values(#{questionNo},#{questionTitle},#{questionContent},#{questionFlag},#{userNo},#{projectNo})
	</insert>
</mapper>









